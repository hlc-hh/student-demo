<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czjt.mapper.CartMapper">

    <!-- 添加商品到购物车 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, product_id, quantity, checked, create_time, update_time)
        VALUES (#{userId}, #{productId}, #{quantity}, #{checked}, NOW(), NOW())
    </insert>

    <!-- 查询用户购物车列表 -->
    <select id="findByUserId" resultType="com.czjt.pojo.Cart">
        SELECT * FROM cart WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID和商品ID查询购物车项 -->
    <select id="findByUserIdAndProductId" resultType="com.czjt.pojo.Cart">
        SELECT * FROM cart
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <!-- 更新商品数量 -->
    <update id="updateQuantity">
        UPDATE cart
        SET quantity = #{quantity}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 增加商品数量（用于已存在商品再次添加） -->
    <update id="increaseQuantity">
        UPDATE cart
        SET quantity = quantity + #{quantity}, update_time = NOW()
        WHERE user_id = #{userId} AND product_id = #{productId}
    </update>

    <!-- 更新选中状态 -->
    <update id="updateChecked">
        UPDATE cart
        SET checked = #{checked}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除购物车项 -->
    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <!-- 根据用户ID和商品ID删除购物车项 -->
    <delete id="deleteByUserIdAndProductId">
        DELETE FROM cart
        WHERE user_id = #{userId} AND product_id = #{productId}
    </delete>

    <!-- 清空用户购物车 -->
    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

    <!-- 查询用户选中的购物车项 -->
    <select id="findCheckedByUserId" resultType="com.czjt.pojo.Cart">
        SELECT * FROM cart
        WHERE user_id = #{userId} AND checked = 1
        ORDER BY create_time DESC
    </select>

    <!-- 批量删除购物车项 -->
    <delete id="batchDelete">
        DELETE FROM cart WHERE id IN
        <foreach collection="cartIds" item="cartId" open="(" separator="," close=")">
            #{cartId}
        </foreach>
    </delete>

    <!-- 批量更新选中状态 -->
    <update id="batchUpdateChecked">
        UPDATE cart SET checked = #{checked}, update_time = NOW()
        WHERE id IN
        <foreach collection="cartIds" item="cartId" open="(" separator="," close=")">
            #{cartId}
        </foreach>
    </update>

</mapper>