<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czjt.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.czjt.pojo.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalPrice" column="total_price"/>
        <result property="payPrice" column="pay_price"/>
        <result property="freightPrice" column="freight_price"/>
        <result property="status" column="status"/>
        <result property="paymentTime" column="payment_time"/>
        <result property="deliveryTime" column="delivery_time"/>
        <result property="receiveTime" column="receive_time"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="receiverAddress" column="receiver_address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="refundStatus" column="refund_status"/>
        <result property="refundApplyTime" column="refund_apply_time"/>
        <result property="refundCompleteTime" column="refund_complete_time"/>
        <result property="refundReason" column="refund_reason"/>
    </resultMap>

    <select id="findByUserId" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, total_price, pay_price, freight_price,
                           status, receiver_name, receiver_phone, receiver_address,
                           refund_status, create_time, update_time)
        VALUES (#{orderNo}, #{userId}, #{totalPrice}, #{payPrice}, #{freightPrice},
                #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress},
                #{refundStatus}, NOW(), NOW())
    </insert>

    <update id="updateStatus">
        UPDATE orders SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateRefundStatus">
        UPDATE orders
        SET refund_status = #{refundStatus},
            refund_reason = #{refundReason},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM orders WHERE user_id = #{userId}
    </select>

</mapper>
